[project]
name = "demo-durable-workflow"
version = "0.1.0"
description = "M8 durable-orchestration demo (Temporal Python SDK worker + starter)"
requires-python = ">=3.11"
dependencies = ["temporalio>=1.32.0,<2"]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
